/**
 * Schémas de validation Zod pour FleetMaster Pro
 * 
 * @deprecated Ce fichier est maintenu pour compatibilité. 
 *             Importez directement depuis '@/lib/schemas' pour les nouveaux développements.
 * 
 * Exemple de migration :
 *   AVANT : import { createVehicleSchema } from '@/lib/validation/schemas'
 *   APRÈS : import { createVehicleSchema } from '@/lib/schemas'
 */

// Re-export de tous les schémas depuis le fichier canonique
export * from '../schemas';
